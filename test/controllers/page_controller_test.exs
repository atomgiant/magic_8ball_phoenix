defmodule Magic8ballPhoenix.PageControllerTest do
  use Magic8ballPhoenix.ConnCase

  test "GET /" do
    # conn = get conn(), "/"
    # assert html_response(conn, 200) =~ "Welcome to Phoenix!"
  end
end
